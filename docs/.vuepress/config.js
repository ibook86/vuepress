// .vuepress/config.js
const {config} = require("vuepress-theme-hope");

module.exports = config({
    // your config here
    themeConfig: {
        logo: "/logo.svg",
        hostname: "https://vuepress-pi.vercel.app",
    }
});